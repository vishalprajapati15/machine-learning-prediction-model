# -*- coding: utf-8 -*-
"""Regression Model

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1hFBjj03OteQ9h8NRLIwUNsND7MuaElBP
"""

from google.colab import drive
drive.mount('/content/drive')

import pandas as pd
import numpy as np

file_path = '/content/drive/My Drive/Lab/modified_placement_data.csv'
df = pd.read_csv(file_path)
print(df.head())
df.shape
df.info()

file_path = '/content/drive/My Drive/Lab/placement.csv'
df = pd.read_csv(file_path)
print(df)
df.shape
df.info()

import matplotlib.pyplot as plt

plt.scatter(df['cgpa'], df['iq'], c= df['placement'])

x = df.iloc[:,1:3]
y = df.iloc[:,-1:]
print(x)
print(y)

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(x,y,test_size=0.1)

x_train

x_test

y_train

y_test

from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()

x_train = scaler.fit_transform(x_train)

x_train

x_test = scaler.transform(x_test)

x_test

#y_train = scaler.transform(y_train)
y_train = y_train.values.ravel()

y_train

y_test = y_test.values.ravel()

y_test

from sklearn.linear_model import LogisticRegression

clf = LogisticRegression()

clf.fit(x_train, y_train)

y_pred = clf.predict(x_test)

y_pred

y_test

from sklearn.metrics import accuracy_score

accuracy_score(y_test, y_pred)

from mlxtend.plotting import plot_decision_regions

plot_decision_regions(x_train , y_train, clf = clf, legend = 2)

import pickle

pickle.dump(clf,open('model.pkl', 'wb'))

with open("model.py", "w") as f:
    f.write("""# Your model code here""") # write your python code code

from google.colab import files
files.download("model.py")          # download your file

